module IfTest
  unless (final_path ~ /.*\.flv$/)
  end

  if (not params[:recipients].empty?)
  end
  
  if (not params[:uid].blank?) # find someone'
  end
  
  if (not search_string.empty?)
  end
end

