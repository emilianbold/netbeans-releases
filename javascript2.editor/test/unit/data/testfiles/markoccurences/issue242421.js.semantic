var |>METHOD:myF<| = function |>METHOD:MyLib_Function<| (path, ref, pfx, options) {
   var v = expand(path, ref, pfx, options);
    
   if( options && options.option1 && (v === path) )
       return path;

   v = |>GLOBAL:page<|.root + '~/' + v;

   return v;
};