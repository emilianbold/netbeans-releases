var |>GLOBAL:Core<| = |>GLOBAL:Core<| || {};

|>GLOBAL:Core<|.needModules(function() {
    "use strict";

    var REGEXP = /[+]?\d{1,20}$/; // REGEXP marked as unused

    |>GLOBAL:Core<|.|>CLASS:Test<| = {
        |>METHOD:test<|: function(value) {
            var fieldType; // fieldType marked as unused

            alert(REGEXP.test(value));

            switch (fieldType) {
                case "test":
                    alert(REGEXP.test(value));
                    break;
            }
        }
    };   
});
