function |>METHOD:diff<|(data) {
    var offset = 0;
    for (var i = 0; i < |>GLOBAL:rows<|; i++) {
        var |>CLASS:team<| = data[i+offset]; // mark occurrences or rename|refactor team
        if (team.|>FIELD:name<| === "Plugin Portal UC") {
            if (i + 1 < data.length) {
                team = data[i + 1];
                offset = offset+1;
            } else { // fallback
                team = {
                    "|>FIELD:name<|": "",
                    "|>FIELD:bugSets<|": []
                };
            }
        }
    }
}
