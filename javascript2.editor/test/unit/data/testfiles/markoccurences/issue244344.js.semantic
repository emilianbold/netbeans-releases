var |>GLOBAL:SeLiteMisc<|= {};
|>GLOBAL:SeLiteMisc<|.|>METHOD:isInstance<|= function |>METHOD:isPrdca<|( object, classes, className, message ) {
    var |>UNUSED:arr<|= [object, classes, message, className ];
    
    if( typeof classes==='function' ) {
        classes= [classes];
    }
    for( var i=0; i<classes.length; i++ ) {
        var |>UNUSED:clazz<|= classes[i];
        if( object ) {
        }
    }    
    return false; 
};