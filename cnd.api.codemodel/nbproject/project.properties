is.autoload=true
javac.source=1.7
javac.compilerargs=-Xlint -Xlint:-serial

#TODO:

# CMCursor: clang_getCursorReferenceNameRange, clang_getEnumConstantDeclUnsignedValue
#           clang_getDeclObjCTypeEncoding, 
# stopped at clang_Cursor_isBitField

# CMSourceRange: range from start/end source location
# CMFile 
# CMIndex

# introduce set/get user properties to mirror clang_index_set/get* functions

# CMDiagnostic has getOption which should be split by getEnableOption/getDisableOption

#  // Translate the given source location to make it point at the beginning of
#  // the token under the cursor.
#  SLoc = Lexer::GetBeginningOfToken(SLoc, CXXUnit->getSourceManager(),
#                                    CXXUnit->getASTContext().getLangOpts());


# CMDiagnostic getDetails is clang_getChildDiagnostics

# CMFileImplementation/CMFile getURI() and getFilePath():
# is getFilePath() derived? should it be removed and replaced by getURI().getPath()?
