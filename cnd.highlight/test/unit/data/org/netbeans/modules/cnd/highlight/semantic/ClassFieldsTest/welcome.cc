#include "welcome.h"

#define XX 12

Welcome::Welcome(int field) {
    field(field);
}

NotWelcome::antifoo() {
    contrafield = field;
}

void Welcome::foo() {
    field = XX;
    char * st = "field"; // field
    int field2 = field;
    int s(field); 
    s += field;
    field = boo(field);
    int field = 3;
    field = this.field;
    field = boo(field);
}; 

int Welcome::boo(int param) {
    return field + param;
}

int x = field;

int main() {
    int x = field;
}

 