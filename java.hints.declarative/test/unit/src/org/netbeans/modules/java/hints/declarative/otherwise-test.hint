   assert $cond;
=> assert $right != $left; :: matchesWithBind($cond, "$left == $right")
=> assert $right == $left; :: matchesWithBind($cond, "$left != $right")
=> assert !$cond           :: otherwise
;;
