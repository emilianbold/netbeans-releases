#define EXPECT { int Exit_Flag; Exit_Flag = false; \
 while (!Exit_Flag) { switch (i) {
#define END_EXPECT } } }

int main() {
    int i;
    EXPECT
    case 1:
    for (char *ptr = 0; *ptr != 0; ptr++) {
    }
    END_EXPECT
    return 1;
}
