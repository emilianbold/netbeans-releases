<?xml version="1.0" encoding="UTF-8"?>
<!--
The contents of this file are subject to the terms of the Common Development
and Distribution License (the License). You may not use this file except in
compliance with the License.

You can obtain a copy of the License at http://www.netbeans.org/cddl.html
or http://www.netbeans.org/cddl.txt.

When distributing Covered Code, include this CDDL Header Notice in each file
and include the License file at http://www.netbeans.org/cddl.txt.
If applicable, add the following below the CDDL Header, with the fields
enclosed by brackets [] replaced by your own identifying information:
"Portions Copyrighted [year] [name of copyright owner]"

The Original Software is NetBeans. The Initial Developer of the Original
Software is Sun Microsystems, Inc. Portions Copyright 1997-2006 Sun
Microsystems, Inc. All Rights Reserved.

$Id$
-->
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<project name="NetBeans_Installer_Engine" default="default" basedir=".">
    <property file="build.properties"/>
    
    <description>Builds, tests, and runs the project NetBeans Installer Engine.</description>
    <import file="nbproject/build-impl.xml"/>
    <import file="nbproject/profiler-build-impl.xml"/>
    
    <target name="-post-clean" depends="native-clean"/>
    <target name="-pre-jar" depends="native-build"/>
    
    <target name="native-init" depends="define-platform,init-platform" description="Inits the native library properties">
        <property file="nbproject/project.properties"/>
    </target>
    
    <target name="native-clean" depends="native-init">
        <exec executable="make" dir="${native.dir}">
            <arg value="clean"/>
        </exec>    
    </target>    
    
    <target name="native-compile" depends="native-init,native-clean">
        <exec executable="make" dir="${native.dir}">
            <arg value="build"/>
        </exec>
    </target>
    
    <target name="native-build" depends="native-init,native-compile">
        <copy file="${native.dir.windows}/${native.dist.dir}/${native.file.windows}" todir="${build.classes.dir}/${native.dir}"/>
        <copy file="${native.dir.linux}/${native.dist.dir}/${native.file.linux}" todir="${build.classes.dir}/${native.dir}"/>
        <copy file="${native.dir.solaris-x86}/${native.dist.dir}/${native.file.solaris-x86}" todir="${build.classes.dir}/${native.dir}"/>
        <copy file="${native.dir.solaris-sparc}/${native.dist.dir}/${native.file.solaris-sparc}" todir="${build.classes.dir}/${native.dir}"/>
        <copy file="${native.dir.macosx}/${native.dist.dir}/${native.file.macosx}" todir="${build.classes.dir}/${native.dir}"/>
    </target>

    <target name="define-platform">
        <condition property="is.windows">
            <contains string="${os.name}" substring="Windows"/>
        </condition>
        <condition property="is.linux">
            <contains string="${os.name}" substring="Linux"/>
        </condition>
        <condition property="is.solaris-x86">
            <and>
                <contains string="${os.name}" substring="SunOS"/>
                <contains string="${os.arch}" substring="x86"/>
            </and>
        </condition>
        <condition property="is.solaris-sparc">
            <and>
                <contains string="${os.name}" substring="SunOS"/>
                <contains string="${os.arch}" substring="sparc"/>
            </and>
        </condition>
        <condition property="is.macosx">
            <or>
                <contains string="${os.name}" substring="Darwin"/>
                <contains string="${os.name}" substring="Mac OS X"/>
            </or>
        </condition>
    </target>
    
    <target name="init-platform" depends="init-windows,init-linux,init-solaris-x86,init-solaris-sparc,init-macosx"/>
   
    <target name="init-windows" if="is.windows">
        <property name="native.dir" value="${native.dir.windows}"/>
        <property name="native.file" value="${native.file.windows}"/>
    </target>
    <target name="init-linux" if="is.linux">
        <property name="native.dir" value="${native.dir.linux}"/>
        <property name="native.file" value="${native.file.linux}"/>
    </target>
    <target name="init-solaris-x86" if="is.solaris-x86">
        <property name="native.dir" value="${native.dir.solaris-x86}"/>
        <property name="native.file" value="${native.file.solaris-x86}"/>
    </target>
    <target name="init-solaris-sparc" if="is.solaris-sparc">
        <property name="native.dir" value="${native.dir.solaris-sparc}"/>
        <property name="native.file" value="${native.file.solaris-sparc}"/>
    </target>
    <target name="init-macosx" if="is.macosx">
        <property name="native.dir" value="${native.dir.macosx}"/>
        <property name="native.file" value="${native.file.macosx}"/>
    </target>
    
    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

    -pre-init:                 called before initialization of project properties
    -post-init:                called after initialization of project properties
    -pre-compile:              called before javac compilation
    -post-compile:             called after javac compilation
    -pre-compile-single:       called before javac compilation of single file
    -post-compile-single:      called after javac compilation of single file
    -pre-compile-test:         called before javac compilation of JUnit tests
    -post-compile-test:        called after javac compilation of JUnit tests
    -pre-compile-test-single:  called before javac compilation of single JUnit test
    -post-compile-test-single: called after javac compilation of single JUunit test
    -pre-jar:                  called before JAR building
    -post-jar:                 called after JAR building
    -post-clean:               called after cleaning build products

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of inserting an obfuscator after compilation could look like this:

    <target name="-post-compile">
    <obfuscate>
    <fileset dir="${build.classes.dir}"/>
    </obfuscate>
    </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Another way to customize the build is by overriding existing main targets.
    The targets of interest are: 

    -init-macrodef-javac:     defines macro for javac compilation
    -init-macrodef-junit:     defines macro for junit execution
    -init-macrodef-debug:     defines macro for class debugging
    -init-macrodef-java:      defines macro for class execution
    -do-jar-with-manifest:    JAR building (if you are using a manifest)
    -do-jar-without-manifest: JAR building (if you are not using a manifest)
    run:                      execution of project 
    -javadoc-build:           Javadoc generation
    test-report:              JUnit report generation

    An example of overriding the target for project execution could look like this:

    <target name="run" depends="NetBeans_Installer_Engine-impl.jar">
    <exec dir="bin" executable="launcher.exe">
    <arg file="${dist.jar}"/>
    </exec>
    </target>

    Notice that the overridden target depends on the jar target and not only on 
    the compile target as the regular run target does. Again, for a list of available 
    properties which you can use, check the target you are overriding in the
    nbproject/build-impl.xml file. 

    -->
</project>
