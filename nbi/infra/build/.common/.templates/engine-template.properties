# directory where the build script library and properties reside
common.dir=${basedir}/../.common

# name of the engine distributive file
engine.jar.file=nbi-engine.jar

# url of the nbi administration server where the engine should be released
release.url=http://localhost/nbi/admin

sources.dir=

#
jarsigner.keystore=
jarsigner.storepass=
jarsigner.alias=
