<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." default="netbeans" name="javacard.ri.bundle">
    <description>Builds, tests, and runs the project org.netbeans.modules.javacard.ri.bundle</description>
    <import file="../nbbuild/templates/projectized.xml"/>

    <!--
    <property name="javacard-runtime-basedir" value="JCDK3.0.1_ConnectedEdition"/>
    <property name="runtime-zip" value="${basedir}/external/javacard_ri.zip"/>
    <property name="runtime-dest" value="${basedir}/release/"/>

    <available file="${runtime-zip}"  property="runtime-found"/>
    <available file="${runtime-dest}/${javacard-runtime-basedir}" property="runtime-unpacked"/>

    <target name="init" depends="harness.init,unzip-runtime"/>

    <target name="unzip-runtime" unless="${runtime-unpacked}">
        <fail unless="runtime-found">
Java Card Runtime zip not found at ${runtime-zip}.
        </fail>
        <mkdir dir="${runtime-dest}"/>
        <unzip src="${runtime-zip}" overwrite="true" dest="${runtime-dest}"/>
    </target>

    <target name="clean" depends="projectized-common.clean,clean-runtime"/>

    <target name="clean-runtime">
        <delete dir="${runtime-dest}"/>
    </target>
-->
</project>
