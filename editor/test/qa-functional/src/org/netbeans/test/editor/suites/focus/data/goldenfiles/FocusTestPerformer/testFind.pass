package org.netbeans.test.editor.suites.focus.data.testfiles.FocusTestPerformer;

import java.util.Arrays;

/*
 * SysProps.java
 *  INSERTING TEXT 
 * Created on February 3, 2003, 2:26 PM
 */

/**
 *  INSERTING TEXT 
 * @author  eh103527
 */
public class Test2 {
    
    /** Creates a new instance of SysProps */
    public Test2() {  // INSERTING TEXT 
    }
    
    /**
     *  INSERTING TEXT 
     * @param args the command line arguments
     */
    public static void main(String[] args) {  // INSERTING TEXT 
        Object keys[]=System.getProperties().keySet().toArray(); // INSERTING TEXT 
        Arrays.sort(keys); // INSERTING TEXT 
        for (int i=0;i < keys.length;i++) {  // INSERTING TEXT 
            System.out.println(keys[i]+"="+System.getProperty((String)keys[i]));  // INSERTING TEXT 
        }
    }
    
}
