Code completion result for source line:
arr.each { |_| Huh.err| }
(QueryType=COMPLETION, prefixSearch=true, caseSensitive=true)
CALL       each { }                                   Dir
CALL       each(*args) { }                            Zlib::GzipReader
CALL       each(sep_string) { }                       IO
CALL       each(separator) { }                        String
------------------------------------
METHOD     errors                          [STATIC]   OpenSSL
