<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." default="netbeans" name="swing.validation">
    <description>Builds, tests, and runs the project org.netbeans.modules.swing.validation</description>
    <import file="../nbbuild/templates/projectized.xml"/>

    <target name="jar" depends="projectized-common.jar">
        <!-- WTF? -->
        <echo>Copying the ValidationAPI JAR to ${cluster}/modules/ext/</echo>
        <mkdir dir="${cluster}/modules/ext"/>
        <copy file="${basedir}/external/ValidationAPI.jar" todir="${cluster}/modules/ext/"/>
    </target>


</project>
