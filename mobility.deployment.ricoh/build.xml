<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." default="netbeans" name="mobility/deployment/ricoh">
    <description>Builds, tests, and runs the project ricoh deploymentt</description>
    <import file="../../../nbbuild/templates/projectized.xml"/>

    <target name="netbeans-extra" depends="release"/>

    <target name="release" depends="init">
        <mkdir dir="${cluster}/modules/scr"/>
        <copy todir="${cluster}/modules/scr">
            <fileset dir="scripts"/>
        </copy>
    </target>	
</project>
