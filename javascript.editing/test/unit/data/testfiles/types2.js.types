Axt.<Axt.Template>Template</Axt.Template>.<String>prototype</String> = {
    /**
     * Applies bla bla bla bla
     * foo foo bar bar
     * @param {Mixed} el The bla bla bla
     * @param {Object/Array} values The bla bla bloo (i.e. {0}) or an
     * object (i.e. {foo: 'bar'})
     * @param {Boolean} returnElement (optional) true to return a Axt.Element (defaults to blii)
     * @return {HTMLElement/Axt.Element} The new node or Alement
     * @private
     * @constructor
     * @deprecated
     */
    insertBeeefore: <Function>function(<Mixed>el</Mixed>, <Object|Array>values</Object|Array>, <Boolean>returnElement</Boolean>){
        // Initial
        <String>returnElement</String> = <String>"hello"</String>;
        <RegExp>values</RegExp> = <RegExp>/foo/</RegExp>;
        return this.doInsert(<String>'beforeBegin'</String>, <Mixed>el</Mixed>, <RegExp>values</RegExp>, <String>returnElement</String>);
    }</Function>,
    /** @ignore */
    usage: <Function>function() {
        mycall = <insertBeeefore>insertBeeefore(<String>""</String>, <String>""</String>, <String>""</String>)</insertBeeefore>;
        <alert>alert(mycall)</alert>;
    }</Function>
};


