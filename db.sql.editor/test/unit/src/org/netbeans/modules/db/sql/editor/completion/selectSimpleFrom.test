# The "customers" schema should show up, the "customer" table too. The "customization" table should not,
# because it's not in the default schema.

select cus| from customers.customer, customizations.customization

--
customers*
  customer
    customer_id
    address_line
  discount_code
    id
customizations
  customization
    invoice_id
    customer_id
