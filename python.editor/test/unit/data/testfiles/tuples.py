def foo():
    for x, y in None:
        print y
    for z in None:
        pass
    (a, b) = None
    c = None
    print b
    for dirpath, dirnames, filenames in os.walk(src): # dirnames unused
    for i, (name, path) in enumerate(result): # i unused
        pass
    aliases, entry = cmdutil.findcmd(command, table) # aliases unused
    for rev, node, fnode in self._hgtagsnodes(): # rev & node unused
        pass

