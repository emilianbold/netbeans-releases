package @@PACKAGE_NAME@@;

import java.beans.PropertyChangeListener;
import javax.swing.JComponent;
import org.netbeans.spi.options.OptionsPanelController;
import org.openide.util.HelpCtx;
import org.openide.util.Lookup;

  
public final class @@OptionsPanelController_CLASS_NAME@@  extends OptionsPanelController {
  private @@Panel_CLASS_NAME@@ panel;     
        
    public void update () {
        getPanel().update();
    }
    
    public void applyChanges () {
        getPanel().applyChanges ();
    }
    
    public void cancel () {
        getPanel().cancel();
    }
    
    public boolean isValid () {
        return getPanel().dataValid ();
    }
    
    public boolean isChanged () {
        return getPanel().isChanged();
    }
    
    public HelpCtx getHelpCtx () {
        return new HelpCtx ("netbeans.optionsDialog.@@MODULE_CNB@@");
    }
    
    public JComponent getComponent (Lookup masterLookup) {
        return getPanel();
    }

    public void addPropertyChangeListener(PropertyChangeListener l) {
        getPanel().addPropertyChangeListener(l);
    }

    public void removePropertyChangeListener(PropertyChangeListener l) {
        getPanel().removePropertyChangeListener(l);
    }

private @@Panel_CLASS_NAME@@ getPanel() {
        if (panel == null) {
            panel = new @@Panel_CLASS_NAME@@ ();                        
        }
        return panel;
    }
}